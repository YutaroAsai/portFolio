// @ts-check
import { defineConfig } from 'astro/config';

// Astroの設定ファイル。
// https://astro.build/config
// 中身が空 = 全てデフォルト設定のまま(出力先: dist/、SSRなしの静的サイトビルド等)。
// 独自ドメイン運用時に site: "https://example.com" のような項目を足すことが多い。
export default defineConfig({});
